services:
  - type: web
    name: premium-video-app
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
    headers:
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN
